/**
 * @fileoverview Barrel file for the MobileNavigation component.
 * This file primarily exports the MobileNavigation component for easy import.
 */

import MobileNavigation from "./MobileNavigation";

// Default export is the MobileNavigation component.
export default MobileNavigation;

// If MobileNavMenu needs to be imported directly elsewhere in the future,
// it can be exported as a named export:
// export { default as MobileNavMenu } from './MobileNavMenu';
